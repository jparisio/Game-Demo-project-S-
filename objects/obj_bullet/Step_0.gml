//var bullet_trail = instance_create_layer(x, y, "Instances", obj_trail)
//bullet_trail.image_blend = c_yellow;
//bullet_trail.sprite_index = sprite_index;
//bullet_trail.x = x - 2 * sign(speed);
image_angle = direction;
//part_particles_create(particle_system, x, y, particle_trail, 1);

if speed != 0 move_dir = sign(speed);